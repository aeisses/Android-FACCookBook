<?xml version="1.0" encoding="utf-8"?>
<manifest package="github.com.foodactioncommitteecookbook"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name="github.com.foodactioncommitteecookbook.CookbookApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:name=".TitleActivity_"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity_"/>
        <activity android:name=".ListActivity_"/>
        <activity android:name=".LocationActivity_"/>
        <activity android:name=".map.MapActivity_"/>
        <activity android:name=".PurchaseActivity_"/>
        <activity android:name=".RecipeActivity_"/>
        <activity
            android:name=".SearchActivity_"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH"/>
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable"/>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBl_0P4TkaP_oe5eeL_KFPEWKOugCBhu_A"/>
    </application>

</manifest>
